# 项目配置
project:
  name: "基于知识图谱的变速箱故障预测系统"
  data_path: "data/raw/XJTU_Gearbox"
  output_path: "results"

# Neo4j配置
neo4j:
  uri: "bolt://localhost:7687"
  username: "neo4j"
  password: "${NEO4J_PASSWORD}"  # 从环境变量读取: export NEO4J_PASSWORD=your_password
  container_name: "neo4j"

# 数据预处理配置
preprocessing:
  window_size: 1024
  overlap: 512
  sampling_rate: 25600
  features:
    - time_domain: ["mean", "std", "rms", "max", "min", "peak", "kurtosis", "skewness"]
    - frequency_domain: ["spectral_centroid", "spectral_energy", "spectral_entropy"]

# 知识图谱配置
knowledge_graph:
  node_types:
    - Fault
    - Component
    - Signal
    - Feature
  edge_types:
    - CAUSED_BY
    - AFFECTS
    - HAS_FEATURE
    - LOCATED_AT
    - SIMILAR_TO

# 模型配置
models:
  gnn:
    hidden_dim: 256
    num_layers: 4
    dropout: 0.3
    learning_rate: 0.001
    epochs: 100
    batch_size: 256

  mlp:
    hidden_dim: 128
    dropout: 0.3
    learning_rate: 0.001
    epochs: 100

  kg_enhanced_mlp:
    hidden_dim: 128
    kg_embedding_dim: 64
    dropout: 0.3
    learning_rate: 0.001
    epochs: 100

  kg_enhanced_mlp_v2:
    hidden_dim: 128
    kg_embedding_dim: 64
    dropout: 0.3
    learning_rate: 0.001
    epochs: 100

  cnn:
    hidden_channels: 64
    dropout: 0.3
    learning_rate: 0.001
    epochs: 100

  cnn_kg:
    hidden_channels: 64
    kg_embedding_dim: 33
    dropout: 0.3
    learning_rate: 0.001
    epochs: 100

  cnn_kg_v2:
    hidden_channels: 64
    kg_embedding_dim: 33
    dropout: 0.3
    learning_rate: 0.001
    epochs: 100

  cnn_kg_v3:
    hidden_channels: 64
    kg_embedding_dim: 33
    dropout: 0.2
    learning_rate: 0.001
    epochs: 100

  gnn_kg_v2:
    hidden_dim: 256
    num_layers: 3
    kg_embedding_dim: 33
    dropout: 0.3
    learning_rate: 0.001
    epochs: 100
    batch_size: 256

# 训练配置
training:
  test_size: 0.2
  val_size: 0.1
  random_seed: 42
  early_stopping_patience: 30

# 可视化配置
visualization:
  figure_size: [12, 8]
  dpi: 300
  style: "darkgrid"
